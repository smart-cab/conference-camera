# Используем официальный образ Go как базовый образ
FROM golang:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы зависимостей Go и сборочный файл в контейнер
COPY go.mod .
COPY go.sum .
COPY .env .

# Загружаем зависимости
RUN go mod download

# Копируем исходный код проекта в контейнер
COPY . .

# Собираем исполняемый файл
RUN go build -o main .

# Экспортируем порт, который будет слушать ваш сервер
EXPOSE 8888

# Запускаем приложение
CMD ["./main"]
